---
import { LATEST_AS_RELEASE } from '@lib/latestArchivesSpaceRelease.ts'

const { htmlUrl, tagName } = LATEST_AS_RELEASE
---

<p class="latest-release-blurb">
  The latest release of ArchivesSpace is <a href={htmlUrl}>{tagName}</a>.
</p>
